services:
  check-status:
    build: .
    image: local/check-status:latest
    init: true
    restart: unless-stopped
    env_file: .env
    environment:
      - TZ=Europe/Madrid
    volumes:
      # Snapshot persists across container restarts on the host filesystem.
      - ./data:/data
